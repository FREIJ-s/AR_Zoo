<form method="post" action="{{ path('app_info_animal_delete', {'id': info_animal.id}) }}" 
    onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet enregistrement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_animal.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
