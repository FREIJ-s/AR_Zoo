{% extends 'base.html.twig' %}

{% block title %}Détails de l'Animal{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center text-info">Détails de l'Animal 🦒</h1>

    <div class="card p-4 shadow">
        <table class="table">
            <tbody>
                <tr>
                    <th scope="row">ID</th>
                    <td>{{ animal.id }}</td>
                </tr>
                <tr>
                    <th scope="row">Nom</th>
                    <td>{{ animal.name }}</td>
                </tr>
                <tr>
                    <th scope="row">Date de Création</th>
                    <td>{{ animal.creationDate ? animal.creationDate|date('Y-m-d H:i:s') : 'Non spécifiée' }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('app_animal_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
        <a href="{{ path('app_animal_edit', {'id': animal.id}) }}" class="btn btn-warning">Modifier</a>
        {{ include('animal/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
