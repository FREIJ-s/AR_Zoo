{% extends 'base.html.twig' %}

{% block title %}Liste des Animaux{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center text-primary">Nos Animaux 🐘🦁🐆</h1>

    <table class="table table-striped table-hover mt-4">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Date de Création</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for animal in animals %}
            <tr>
                <td>{{ animal.id }}</td>
                <td>{{ animal.name }}</td>
                <td>{{ animal.creationDate ? animal.creationDate|date('Y-m-d H:i:s') : 'Non spécifiée' }}</td>
                <td>
                    <a href="{{ path('app_animal_show', {'id': animal.id}) }}" class="btn btn-info btn-sm">Voir</a>
                    <a href="{{ path('app_animal_edit', {'id': animal.id}) }}" class="btn btn-warning btn-sm">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4" class="text-center">Aucun animal trouvé.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_animal_new') }}" class="btn btn-success">Ajouter un nouvel animal</a>
</div>
{% endblock %}
