{% extends 'base.html.twig' %}

{% block title %}Ajouter une Image d'Habitat{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center text-success">Ajouter une Nouvelle Image 🏝️</h1>

    <div class="card p-4 shadow-lg">
        {{ include('image_habitat/_form.html.twig', {'button_label': 'Ajouter'}) }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_image_habitat_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
    </div>
</div>
{% endblock %}
