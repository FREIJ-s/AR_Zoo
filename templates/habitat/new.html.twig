{% extends 'base.html.twig' %}

{% block title %}Ajouter un Habitat{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center text-success">Ajouter un Nouvel Habitat 🌍</h1>

    <div class="card p-4 shadow-lg">
        {{ include('habitat/_form.html.twig', {'button_label': 'Ajouter'}) }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_habitat_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
    </div>
</div>
{% endblock %}
