<form method="post" action="{{ path('app_info_animal_delete', {'id': info_animal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_animal.id) }}">
    <button class="btn">Delete</button>
</form>
