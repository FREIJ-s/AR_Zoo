{% extends 'base.html.twig' %}

{% block title %}Modifier l'Animal{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center text-primary">Modifier un Animal 🦁</h1>

    <div class="card p-4 shadow">
        {{ include('animal/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('app_animal_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
        {{ include('animal/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
