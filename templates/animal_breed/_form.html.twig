{{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.breed, 'Race de l\'animal', { 'class': 'form-label' }) }}
        {{ form_widget(form.breed, { 'attr': {'class': 'form-control'} }) }}
    </div>

    <button type="submit" class="btn btn-success w-100">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
