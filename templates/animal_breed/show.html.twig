{% extends 'base.html.twig' %}

{% block title %}Détails d'une Race{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center text-info">Détails de la Race 🦒</h1>

    <div class="card p-4 shadow-lg">
        <table class="table">
            <tbody>
                <tr>
                    <th scope="row">ID</th>
                    <td>{{ animal_breed.id }}</td>
                </tr>
                <tr>
                    <th scope="row">Nom de la Race</th>
                    <td>{{ animal_breed.breed }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('app_animal_breed_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
        <a href="{{ path('app_animal_breed_edit', {'id': animal_breed.id}) }}" class="btn btn-warning">Modifier</a>
        {{ include('animal_breed/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
