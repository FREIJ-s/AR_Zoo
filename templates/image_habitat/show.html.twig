{% extends 'base.html.twig' %}

{% block title %}ImageHabitat{% endblock %}

{% block body %}
    <h1>ImageHabitat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_habitat.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ image_habitat.label }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ image_habitat.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_habitat_index') }}">back to list</a>

    <a href="{{ path('app_image_habitat_edit', {'id': image_habitat.id}) }}">edit</a>

    {{ include('image_habitat/_delete_form.html.twig') }}
{% endblock %}
