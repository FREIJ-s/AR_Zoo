{% extends 'base.html.twig' %}

{% block title %}ImageHabitat index{% endblock %}

{% block body %}
    <h1>ImageHabitat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>Url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for image_habitat in image_habitats %}
            <tr>
                <td>{{ image_habitat.id }}</td>
                <td>{{ image_habitat.label }}</td>
                <td>{{ image_habitat.url }}</td>
                <td>
                    <a href="{{ path('app_image_habitat_show', {'id': image_habitat.id}) }}">show</a>
                    <a href="{{ path('app_image_habitat_edit', {'id': image_habitat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_image_habitat_new') }}">Create new</a>
{% endblock %}
