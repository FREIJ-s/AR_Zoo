{{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.label, 'Nom de l\'image', { 'class': 'form-label' }) }}
        {{ form_widget(form.label, { 'attr': {'class': 'form-control'} }) }}
    </div>

    <div class="mb-3">
        {{ form_label(form.url, 'URL de l\'image', { 'class': 'form-label' }) }}
        {{ form_widget(form.url, { 'attr': {'class': 'form-control'} }) }}
    </div>

    <button type="submit" class="btn btn-success w-100">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
